// 'use strict';

// module.exports = app => {
//   app.validator.addRule('local_auth_password', (rule, value) => {
//     if (typeof value !== 'string') {
//       return 'password must be a string';
//     }
//     if (value.length < 6) {
//       return 'wrong password';
//     }
//   });
// };
